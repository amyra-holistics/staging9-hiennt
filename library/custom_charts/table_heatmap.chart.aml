CustomChartDef table_heatmap {
  label: 'Table Heatmap'
  description: 'To reveal patterns across days and months by coloring each cell of a calendar grid by a measure value.'
  icon: 'hicon:chart-table-heatmap'
  fields {
    field date {
      label: 'Date'
      type: 'dimension'
      sort {
        apply_order: 1
        direction: 'asc'
      }
      data_type: 'date'
    }
    field value {
      label: 'Value'
      type: 'measure'
      sort {
        apply_order: 2
        direction: 'desc'
      }
    }
  }
  options {
    option show_tooltip {
      label: 'Show tooltip'
      type: 'toggle'
      default_value: true
    }
  }
  template: @vgl
{
    "data": {
      "values": @{values}
    },
    "params": [
      {"name": "normalPointSelection", "select": {"type": "point", "encodings": ["x", "y"], "toggle": "true", "clear": "mouseup"}},
      {"name": "hoverPointSelection", "select": {"type": "point", "encodings": ["x", "y"], "on": "mouseover", "clear": "mouseout"}}
    ],
    "mark": {
      "type": "rect",
      "tooltip": @{options.show_tooltip.value}
    },
    "holisticsConfig": {
      "crossFilterSignals": ["normalPointSelection"],
      "contextMenuSignals": ["hoverPointSelection"]
    },
    "width": {"step": 13},
    "height": {"step": 13},
    "encoding": {
      "x": {
        "type": "ordinal",
        "field": @{fields.date.name},
        "title": "Day",
        "timeUnit": "date",
        "axis": {"format": "%e", "labelAngle": 0, "labelPadding": 8}
      },
      "y": {
        "type": "ordinal",
        "field": @{fields.date.name},
        "title": "Month",
        "timeUnit": "month",
        "axis": {"labelPadding": 8}
      },
      "color": {
        "type": "quantitative",
        "field": @{fields.value.name},
        "scale": {"scheme": "blues"},
        "legend": {
          "title": null
        },
        "aggregate": "max"
      },
    }
  }
;;
}